{
    "info": {
        "name": "detect_objects",
        "type": "eta.core.types.Module",
        "version": "0.1.0",
        "description": "A module that uses an eta.core.learning.ObjectDetector to detect objects in\nvideo(s)",
        "exe": "detect_objects.py"
    },
    "inputs": [
        {
            "name": "video_path",
            "type": "eta.core.types.Video",
            "description": "the input video",
            "required": true
        },
        {
            "name": "input_labels_path",
            "type": "eta.core.types.VideoLabels",
            "description": "an optional input VideoLabels file to which to add the detections",
            "required": false
        }
    ],
    "outputs": [
        {
            "name": "output_labels_path",
            "type": "eta.core.types.VideoLabels",
            "description": "a VideoLabels file containing the detections",
            "required": true
        }
    ],
    "parameters": [
        {
            "name": "detector",
            "type": "eta.core.types.Object",
            "description": "an eta.core.learning.ObjectDetectorConfig describing the object detector to use",
            "required": true
        },
        {
            "name": "objects",
            "type": "eta.core.types.ObjectArray",
            "description": "an array of objects describing the labels and confidence thresholds of objects to detect. If omitted, all detections emitted by the detector are used",
            "required": false,
            "default": null
        }
    ]
}